# GitHub personal access token — needs: repo, pull_requests (read)
GITHUB_TOKEN=ghp_your_token_here

# GitLab personal access token — needs: api (read_api is enough)
GITLAB_TOKEN=glpat-your_token_here

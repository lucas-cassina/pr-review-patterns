# GitHub repositories — format: "owner/repo"
GITHUB_REPOS = [
    # "your-org/your-repo",
]

# GitLab projects — id can be numeric ID or "group/project" URL-encoded path
GITLAB_REPOS = [
      {"id": "polko1/frontend", "name": "frontend"},
  ]


# GitLab instance base URL (use https://gitlab.com for cloud)
GITLAB_BASE_URL = "https://gitlab.com"

# How many days back to look
DAYS_LOOKBACK = 10
